version: '3.4'

services:
  aspnetapp.frontend:
    image: aspnetapp.frontend
    build:
      context: .
      dockerfile: src/aspnetapp.frontend/Dockerfile
      args:
        sdkImage: kdcllc/dotnet
        sdkTag: 2.2-sdk
        runtimeImage: mcr.microsoft.com/dotnet/core/aspnet
        runtimeTag: 2.2
        DOTNET_VERSION: netcore22
        RUNTESTS: "true"
        VERBOSE: "false"

        # sdkImage: kdcllc/dotnet
        # sdkTag: 3.0-sdk
        # runtimeImage: mcr.microsoft.com/dotnet/core/aspnet
        # runtimeTag: 3.0.0-preview6-buster-slim
        # DOTNET_VERSION: netcore30
        # RUNTESTS: "true"
        # VERBOSE: "false"
  aspnetapp.sqldb:
   image: microsoft/mssql-server-linux